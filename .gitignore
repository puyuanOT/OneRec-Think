# Environments
.venv*/
*.pyc
__pycache__/

# Large / generated model artifacts
basemodel/
!basemodel/expand_vocab.py
!basemodel/download_basemodel.py
!basemodel/merge_model.py
train/results/
train/logs/

# Training outputs / checkpoints / logs
*.log
*.ckpt
wandb/
train/wandb/

# Generated data (parquet, downloads, etc.)
data/*.parquet
data/sid_output/**
data/Beauty.pretrain.before_minionerec.json
# Explicitly ignore RA CoT parquets (already covered by data/*.parquet)
data/training_RA_*.parquet
# Keep RA chunked parts tracked
# data/ra_parts/**  (uncomment to ignore parts if desired)

# Keep raw and OAI-generated JSONs (but ignore the large combined summaries file)
!data/Beauty.pretrain.json
data/Beauty.pretrain.with_summaries.json
!data/Beauty.pretrain.with_summaries.part*.json
!data/sequential_data_processed.txt
data/user_summaries.json
!data/user_summaries.part*.json

# Misc
.DS_Store

